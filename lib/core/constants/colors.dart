import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryOrange = Color(0xFFE65100); // Main neon orange accent
  static const Color backgroundDark = Color(0xFF09090B); // Near black background
  static const Color panelDark = Color(0xFF121214); // Slightly lighter black for form container
  static const Color textMain = Colors.white;
  static const Color textMuted = Color(0xFF8E8E93);
  static const Color borderDark = Color(0xFF27272A);
}